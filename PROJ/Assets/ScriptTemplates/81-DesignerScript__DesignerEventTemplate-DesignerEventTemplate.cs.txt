using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DesignerScriptTemplate : MonoBehaviour
{
    /*
     *  - Event types are found in the class IEvent
     *  - If the event you want doesnt exist, talk to a programmer about adding it, and when you want it to be called
     */

    private void OnEnable()
    {
        //EventHandler<DIN EVENTTYP HÄR>.RegisterListener(OnEventCalled);
    }
    private void OnDisable()
    {
        //EventHandler<DIN EVENTTYP HÄR>.UnregisterListener(OnEventCalled);
    }

    private void OnEventCalled(/*DIN EVENTTYP HÄR, thisEvent */)
    {
        //Do stuff
    }
}